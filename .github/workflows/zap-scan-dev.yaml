name: Zap Scanning

on:
  push:
    branches:
      - dev
         
  workflow_dispatch:
    inputs:
      environment:
        description: 'The environment to deploy to'
        required: true
        default: 'dev'
        options:
          - 'dev'
          
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  zap_scan_frontend:
    runs-on: ubuntu-latest
    name: Scan the epd-Frontend 
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-full-scan@v0.4.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://epd-frontend-dev.apps.silver.devops.gov.bc.ca/'

  zap_scan_applications:
    runs-on: ubuntu-latest
    name: Scan the epd-backend-applications
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://epd-backend-applications-dev.apps.silver.devops.gov.bc.ca/graphql'

  zap_scan_gateway:
    runs-on: ubuntu-latest
    name: Scan the epd-backend-gateway
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://epd-backend-gateway-dev.apps.silver.devops.gov.bc.ca/'

  zap_scan_users:
    runs-on: ubuntu-latest
    name: Scan the epd-backend-users
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://epd-backend-users-dev.apps.silver.devops.gov.bc.ca/'
  zap_scan_forms-flow-analytics:
    runs-on: ubuntu-latest
    name: Scan the forms-flow-analytics
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://forms-flow-analytics-e38158-dev.apps.silver.devops.gov.bc.ca/'
  zap_scan_forms-flow-api:
    runs-on: ubuntu-latest
    name: Scan the forms-flow-api
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://forms-flow-api-e38158-dev.apps.silver.devops.gov.bc.ca/'
  zap_scan_forms-flow-bpm:
    runs-on: ubuntu-latest
    name: Scan the forms-flow-bpm
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://forms-flow-bpm-e38158-dev.apps.silver.devops.gov.bc.ca/camunda'
  zap_scan_forms-flow-data-analysis:
    runs-on: ubuntu-latest
    name: Scan the forms-flow-data-analysis
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://forms-flow-data-analysis-e38158-dev.apps.silver.devops.gov.bc.ca/'
  zap_scan_forms-flow-documents:
    runs-on: ubuntu-latest
    name: Scan the forms-flow-documents
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://forms-flow-documents-api-e38158-dev.apps.silver.devops.gov.bc.ca/'
  zap_scan_forms-flow-forms:
    runs-on: ubuntu-latest
    name: Scan the forms-flow-forms
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://forms-flow-forms-e38158-dev.apps.silver.devops.gov.bc.ca/'
  zap_scan_forms-flow-web:
    runs-on: ubuntu-latest
    name: Scan the forms-flow-web
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-api-scan@v0.2.0
        with:
          allow_issue_writing: false
          token: ${{ secrets.GITHUB_TOKEN }}
          issue_title: 'ZAP Scan Report'
          target: 'https://forms-flow-web-e38158-dev.apps.silver.devops.gov.bc.ca/'
  
  

   
  
