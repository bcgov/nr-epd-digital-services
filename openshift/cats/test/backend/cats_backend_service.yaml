apiVersion: v1
kind: Service
metadata:
  name: cats-backend
  namespace: e38158-test
  labels:
    app.kubernetes.io/name: cats-backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: cats-backend
