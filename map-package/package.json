{
  "name": "react-parcelmap-bc",
  "version": "1.1.5",
  "private": false,
  "dependencies": {
    "leaflet": "^1.9.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-leaflet": "^4.2.1"
  },
  "scripts": {
    "build": "SET NODE_ENV=production && rm -rf dist && mkdir dist && npx babel src/map --out-dir dist --copy-files",
    "publish": "rm -rf dist && mkdir dist &&  babel src/map/Map.js -d dist --copy-files"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.26.10",
    "@babel/core": "^7.26.10",
    "@babel/preset-react": "^7.26.10",
    "http-server": "^14.1.1"
  },
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  }
}
