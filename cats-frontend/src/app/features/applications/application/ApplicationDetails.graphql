query getHeaderDetailsByApplicationId($applicationId: Int!) {
  getApplicationDetailsById(id: $applicationId) {
    data {
      id
      siteId
      siteAddress
      siteCity
      appType {
        description
      }
    }
  }
}
